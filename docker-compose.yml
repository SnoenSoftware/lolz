version: "3"

services:
  php:
    image: "ghcr.io/brbkaffe/lolz/fpm:latest"
    volumes:
      - './data:/var/www/lolz/data'
    environment:
      APP_ENV: "prod"

  nginx:
    image: 'ghcr.io/brbkaffe/lolz/proxy:latest'
    ports:
      - 8000:80

  cron:
    image: 'ghcr.io/brbkaffe/lolz/cron:latest'
    volumes:
      - './data:/app/data'
    environment:
      APP_ENV: "prod"
